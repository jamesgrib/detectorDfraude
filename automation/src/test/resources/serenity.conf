serenity {
  project.name = "BancoDigital — Automatización E2E"
  take.screenshots = FOR_FAILURES
  tag.strategy = INTERSECTION
}

environments {
  default {
    api.base.url = "http://localhost:8080"
  }
}
